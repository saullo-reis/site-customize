export interface BoxTypes {
  quantity?: number;
  images?: Array<string>;
  backgroundBox?: string;
  texts?: Array<string>;
}

export interface TextAndImageType {
  text?: string;
  imagePosition?: string;
  image?: string;
}

export interface HeaderTypes {
  background?: string;
  logo?: string;
}